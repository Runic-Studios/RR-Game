main: com.runicrealms.game.plugin.GamePlugin
name: Game
version: '3.0'
api-version: 1.21.4
#loader: com.runicrealms.game.plugin.GamePluginLoader
dependencies:
  server:
    Velagones:
      load: AFTER
      required: true
      join-classpath: true
    NBTAPI:
      load: BEFORE
      required: true
      join-classpath: true
    ProtocolLib:
      load: BEFORE
      required: true
      join-classpath: true

